<div class="form-group">
    <label for="title">Title</label>
    <input required value="<%= quest.title %>" type="text" name="title" id="title" class="form-control">
  </div>
  <div class="form-group">
    <label for="description">Description</label>
    <textarea required name="description" id="description" class="form-control"><%= quest.description %></textarea>
  </div>
  <div class="form-group">
    <label for="task">Task</label>
    <textarea required name="task" id="task" class="form-control"><%= quest.task %></textarea>
  </div>
  <button type="submit" class="saveBtn">save</svg></button>